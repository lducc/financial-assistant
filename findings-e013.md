# Finding addendum — annotation is an auditable pipeline, not a spreadsheet handoff

The project now has a defined path from independent evidence labels to a
reconciliation report.  This protects against a common failure mode in small
proxy benchmarks: silently treating one reviewer’s answers as gold.  The next
state change must come from completed human labels, not another unlabelled
retrieval metric.
