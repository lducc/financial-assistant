# Finding addendum — measuring is now the bottleneck

The system can now reject malformed proxy labels before they contaminate an
evaluation.  The limiting factor is trustworthy annotation, not another
unlabelled baseline.  In particular, evidence IDs must remain dashboard-line
addresses once the exact dashboard OCR artifact is available; local mirror line
addresses are useful engineering references but not final gold by assumption.
