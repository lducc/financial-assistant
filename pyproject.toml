[project]
name = "financial-assistant"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "numpy>=2",
  "pytest>=8,<9",
  "sentencepiece>=0.2",
  "torch>=2.6",
  "transformers>=4.48",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
