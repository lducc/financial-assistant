# E001 results — passed

The pinned snapshot contains 1,012 questions with unique, contiguous IDs from 1
through 1,012 and 1,973 reports with unique report IDs across 100 tickers and
2015–2025. The deterministic filename-derived identity gate passes.

Filename scope is not a closed two-class field: the audit found 957
consolidated, 954 separate, 7 aggregated, and 55 unclassified reports. The
prediction that a strict consolidated/separate parser would drop valid reports
is supported. Unknown scope is retained as explicit metadata.

Raw reproducible output: `data/results/e001_e002/audit.json`.

